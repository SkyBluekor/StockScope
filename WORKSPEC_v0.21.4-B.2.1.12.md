# v0.21.4-B.2.1.12 — Research Workspace UX Refactor

## 목적
과거 성과 검증 화면에 설정·연구 결과·신뢰성·확대 검증·원자료가 한 페이지에 계속 누적되는 구조를 정리한다. 사용자에게 한 번에 하나의 질문만 보여주고, 저장된 연구 데이터와 현재 브라우저 UI 세션 상태를 분리한다.

## 핵심 원칙
- 한 화면 = 한 질문.
- 저장된 결과가 존재해도 새 브라우저 세션은 자동으로 결과 화면으로 이동하지 않는다.
- 연구 결과와 캐시는 유지하되 UI는 설정/연구 실행 상태에서 시작한다.
- 과거 연구는 사용자가 `이전 결과 보기` 또는 `연구 기록`에서 명시적으로 불러온다.
- 세부 지표는 필요한 화면에서만 펼친다.

## Research Workspace
매도 기준 연구 영역을 다음 화면으로 나눈다.
1. 요약 — 현재 결론과 다음 행동
2. 연구 실행 — 연구 조건·실행·진행률
3. 연구 결과 — 전략별 결론과 상세 수치
4. 신뢰성 — 연구 결과의 안정성과 한계
5. 확대 검증 — 20→40/60 종목 재검증
6. 연구 기록 — 과거 저장 실행 조회

## B.2.1.11 선행 요구 통합
- 페이지 새로고침/브라우저 재실행 후 `validationReport`를 자동 복원하지 않는다.
- 초기에는 연구 기록 metadata만 가볍게 조회한다.
- 이전 연구가 존재하면 `이전 연구 결과가 있습니다` 안내와 명시적 불러오기 버튼만 보여준다.
- 사용자가 결과를 불러오거나 현재 세션 연구가 완료됐을 때만 요약/결과 화면을 활성화한다.

## Backend
### 연구 기록 metadata
`ExitPolicyValidationCheckpoint.list_reports(limit)` 추가.
- signature
- 생성 시각
- 연구 기간
- 종목 수
- 최대 보유기간
- summary
- 확대 검증 여부/base signature

### API
`GET /api/backtest/exit-policy-validation/history?limit=N`
- 전체 report JSON 대신 경량 metadata 목록 반환.
- 초기 화면은 이 API를 사용해 이전 결과 존재 여부만 확인.

## Frontend 상태
추가 상태:
- `researchSection`: SUMMARY | RUN | RESULTS | RELIABILITY | EXPANDED | HISTORY
- `researchReportSource`: CURRENT_RUN | PREVIOUS_RUN | null
- `previousResearchMeta`
- `researchHistory`

데이터 상태와 화면 상태를 분리한다. `validationReport` 존재 자체가 탭 전환 조건이 되지 않는다.

## 화면별 요구
### 요약
- 현재 연구 결론
- 실제 적용 기준
- 왜 이런 결론인지
- 연구 결과/신뢰성/확대 검증으로 이동하는 짧은 링크 카드
- 전문 지표 기본 노출 금지

### 연구 실행
- 기존 연구 설정과 진행률만 표시
- 연구 결과·Audit 상세·확대 결과는 숨김

### 연구 결과
- 전략별 기본 행은 전략명/결론/한 줄 이유만 표시
- 수치 비교는 전략별 펼치기로 이동
- 전체 정책 원자료는 추가 상세 영역 유지

### 신뢰성
- 기존 Audit UX를 이 화면에만 표시
- 계산 일관성, 민감 전략, 비교 한계, 다음 확인

### 확대 검증
- 기존 표본/확대 목표/데이터 준비/전체 진행률
- 확대 표본 전후 비교 및 다음 행동
- 신뢰성 화면에 확대 실행 UI를 중복 배치하지 않음

### 연구 기록
- 저장된 연구 실행을 최신순으로 표시
- 종목 수, 기간, 최대 보유기간, 핵심 summary 표시
- `결과 보기` 클릭 시 해당 signature report를 명시적으로 로드

## UI 길이 개선
- Research Workspace navigation을 가로 탭 형태로 제공.
- 관련 없는 섹션 DOM은 사용자 화면에서 숨긴다.
- 전략 결과 카드는 기본 compact details 형태로 변경한다.
- 전체 페이지를 보고서처럼 계속 아래로 쌓지 않는다.

## 변경하지 않는 것
- Exit 정책 계산
- Pareto 선택 규칙
- Net/PF/MDD/Giveback 정의
- 종목별 캐시/확대 검증 계산
- Production mapping
- Look-ahead 방지 규칙

## 완료 조건
1. 새로고침 후 저장된 report가 있어도 자동 결과 화면으로 이동하지 않는다.
2. 이전 결과는 사용자가 명시적으로 불러올 때만 화면 데이터로 로드된다.
3. 연구 Workspace 6개 화면이 독립적으로 전환된다.
4. 연구 실행 화면에 결과/Audit/확대 검증 전체 보고서가 동시에 나오지 않는다.
5. 연구 결과 화면에서 10개 전략은 compact list로 보이고 상세 수치는 펼침 후 확인한다.
6. 신뢰성 상세는 신뢰성 화면에서만 보인다.
7. 확대 검증 실행/결과는 확대 검증 화면에서만 보인다.
8. 과거 연구를 연구 기록에서 signature 단위로 불러올 수 있다.
9. 기존 B.2.1.10 계산 로직과 결과는 변경하지 않는다.

## 패키징
ZIP에는 현재 작업 명세 `WORKSPEC_v0.21.4-B.2.1.12.md` 하나만 포함한다. 과거 WORKSPEC, 임시 디버그 파일, 캐시 파일은 제외한다.

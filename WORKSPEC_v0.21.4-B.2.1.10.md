# StockScope v0.21.4-B.2.1.10 작업 명세

## 작업명
Research Workflow & Expanded Validation Correction

## 목적
연구 기능에서 실제 사용 중 확인된 네 가지 문제를 동시에 보정한다.

1. `최신 확정 데이터로 다시 분석` 이후 무엇이 반영되고 무엇이 달라졌는지 보이지 않는 문제
2. 동일 조건 재검증 및 20→40/60종목 확대 시 이미 계산한 종목을 다시 계산하는 성능 문제
3. 확대 검증 중 내부 전략/정책 진행률이 메인 진행률처럼 바뀌어 전체 완료 정도를 알기 어려운 문제
4. 확대 표본 결과가 `유지 x/10`, `변경 x개` 수준에 머물러 사용자의 실제 판단으로 연결되지 않는 문제

연구의 Entry/Stop/Exit 후보, Pareto 선택 규칙, Net/PF/MDD 계산 정의는 변경하지 않는다.

---

## A. 최신 확정 데이터 재분석

### A-1. 실행 순서
`최신 확정 데이터로 다시 분석` 클릭 시 먼저 해당 시장의 최신 확정 거래일을 확인한다.

- 현재 결과의 `as_of_date`와 최신 확정일이 같음 → 전체 전략 재계산 생략
- 더 최신 확정일이 존재함 → 부족한 최신 데이터 준비 후 새 기준일까지 다시 분석

### A-2. 사용자 결과
재분석 후 아래를 기존 결과와 비교해 변경된 항목만 표시한다.

- 분석 기준일
- 현재 판단
- 가장 가까운 전략
- 충족 조건 수
- 손절 참고가
- 1차 목표가
- 2차 목표가

변경이 없으면 `최신 데이터를 반영했지만 현재 판단은 그대로입니다.`라고 명시한다.
최신 상태라면 `이미 최신 확정 데이터입니다.`를 표시하고 불필요한 전체 재계산을 실행하지 않는다.

---

## B. 연구 결과 재사용 및 성능

### B-1. 연구 조건 fingerprint
종목 하나의 연구 결과에 영향을 주는 조건으로 fingerprint를 만든다.

- 기간
- 시장
- 최소 데이터 보유율
- 초기 자본
- 최대 보유기간
- 거래비용
- 최소 종목/거래 표본 규칙
- Target2 이후 연구기간
- 연구/선택 알고리즘 버전

`max_stocks`는 제외한다. 따라서 동일 조건의 20종목 결과는 40/60종목 확대 연구에서 재사용할 수 있다.

### B-2. 20 → 40 확대
기본 동작은 아래와 같다.

- 기존 20종목 audit 재사용
- 신규 20종목만 계산
- 40종목을 다시 집계하여 전략별 최종 판정 생성

`force_refresh=true`인 `전체 연구 다시 계산`은 기존 결과를 재사용하지 않는다.

### B-3. 제한 병렬 처리
새로 계산해야 하는 종목만 최대 2개 worker로 병렬 처리한다.
각 worker는 독립 `ExitPolicyResearchEngine`을 사용한다.

결과 판정 알고리즘은 기존과 동일해야 하며, 캐시/병렬 처리 여부가 결과를 바꾸면 안 된다.

### B-4. 성능 기록
리포트에 다음 값을 추가한다.

- cache lookup 시간
- 동일 표본 재사용 종목 수
- 다른 크기 표본에서 재사용한 종목 수
- 새 계산 종목 수
- 연구 worker 수
- 시세 로딩 시간
- 연구 계산 시간
- 최종 판정 시간
- 전체 시간

---

## C. 연구 진행률 통합

메인 진행률은 내부 ATR/MA20/Swing Low 단계가 아니라 `처리 완료 종목 / 전체 종목`으로 계산한다.

예: 기존 20종목 재사용 + 신규 5종목 계산 완료 / 40종목 = 62.5%

내부 전략/매도방식 진행률은 `세부 작업 보기` 안으로 이동한다.
내부 작업이 다음 정책으로 넘어가 0%가 되어도 메인 진행률은 절대 감소하지 않는다.

확대 검증 흐름은 사용자 기준으로 다음 5단계로 표현한다.

1. 표본 구성
2. 필요한 시세 준비
3. 종목별 연구
4. 기존 결과와 비교
5. 신뢰성 확인

---

## D. 확대 결과 해석

### D-1. 기본 표시
단순 `같은 결론 x/10`, `결론 변경 x개`보다 자연어 결론을 먼저 표시한다.

### D-2. 해석 유형
- 확대 후 새 기준 후보 발생 → `NEW_CANDIDATE`
- 새 후보 없음 + 모든 결론 유지 → `NO_CANDIDATE_STABLE`
- 판단 보류가 기존 기준 유지로 정리됨 → `BASELINE_STRENGTHENED`
- 그 외 방향이 섞이거나 흔들림 → `MIXED_OR_UNSTABLE`

### D-3. 20종목 vs 확대 표본
기존 연구와 확대 연구의 `새 후보 / 기존 유지 / 판단 보류`를 나란히 보여준다.
결론이 바뀐 전략만 기본 표시하고 동일한 전략은 접어서 확인한다.

### D-4. 다음 행동
- 후보 없음 + 안정/강화 → 현재 1차 목표가 전량 매도 기준 유지
- 새 후보 발생 → 실제 적용 전에 해당 전략 신뢰성 검증
- 혼재/불안정 → 현재 기준 변경 금지, 변경된 전략과 신뢰성 결과 확인

---

## E. 리포트 상태 보존

확대 검증 직후 기존 연구와 확대 연구를 UI에서 분리해 비교할 수 있게 한다.
서버는 signature별 archive 리포트를 유지하고 `/exit-policy-validation/report/{signature}`로 이전 리포트를 조회할 수 있게 한다.

---

## F. 현재 적용 기준 조회 안정성
기존의 재시도 로직을 유지한다.
서버 reload 순간 조회가 실패해도 자동 재시도하며, 계속 실패하는 경우에만 작은 안내와 `다시 확인`을 표시한다.

---

## 변경 예상 파일
- `backend/app/backtest/exit_policy_validation_runner.py`
- `backend/app/backtest/service.py`
- `backend/app/backtest/expanded_sample_validation.py`
- `backend/app/api/backtest.py`
- `backend/tests/test_research_workflow_v0214b2110.py`
- `frontend/src/components/BacktestPanel.tsx`
- `frontend/src/services/api.ts`
- `frontend/src/styles.css`

## 변경 금지
- Entry 규칙
- Stop 규칙
- Target 계산
- Exit 후보 목록
- Pareto 선택 기준
- 동일 봉 Stop 우선 처리
- Look-ahead 방지 규칙
- Production 자동 활성화

## 완료 기준
1. 최신 데이터가 없으면 불필요한 재분석을 하지 않는다.
2. 최신 데이터가 있으면 전/후 변경 내용을 보여준다.
3. 20→40 확대 시 기존 20종목 결과를 재사용한다.
4. 전체 재계산과 재사용 결과의 핵심 연구 결과가 동일하다.
5. 메인 진행률은 종목 기준으로 단조 증가한다.
6. 확대 결과에서 `현재 무엇을 해야 하는가`를 자연어로 바로 알 수 있다.
7. 현재 버전 작업 명세 1개만 overlay에 포함한다.

# WORKSPEC v0.21.4-B.2.3 — Scanner Compact Decision Workspace

## 목적
Scanner 결과를 후보별 전체 상세 카드가 반복되는 긴 보고서형 화면에서, 후보 전체를 빠르게 비교하고 선택한 한 종목만 상세 판단하는 의사결정 워크스페이스로 재구성한다.

## 사용자 문제
- 후보 5개가 모두 확장돼 페이지가 과도하게 길다.
- 종목 간 현재 판단, 현재가, 관심 가격, 손절, 1차 목표를 한눈에 비교하기 어렵다.
- 전략 설명과 과거 근거가 모든 후보에 반복돼 핵심 판단이 묻힌다.
- 페이지를 줄이기 위해 글자를 작게 만드는 방식은 허용하지 않는다.

## 구현 범위
1. 상위 후보를 Compact 비교 행으로 표시한다.
   - 우선순위
   - 종목 / 전략
   - 현재 판단
   - 현재가
   - 관심 가격
   - 손절 참고
   - 1차 목표
2. 후보 행을 선택하면 상세 영역은 한 종목만 표시한다.
3. 선택 상세 정보 순서는 현재 판단 → 왜 후보인가 → 판단 변경 조건 → 핵심 가격 → 전략 설명 → 과거 근거 → 지금 행동 순으로 구성한다.
4. 과거 3년 상세 통계와 부족 조건 전체는 기본 접힘 상태로 둔다.
5. 추가 후보는 사용자가 명시적으로 펼쳤을 때만 Compact 행으로 표시한다.
6. 선택한 후보를 Scanner sessionStorage에 함께 저장해 상세 분석 후 돌아왔을 때 복원한다.
7. 새 Scanner 분석이 완료되면 첫 번째 후보를 기본 선택한다.
8. 1920 / 1440 / 1024 및 좁은 화면에서 가로 스크롤, 한국어 한 글자 단위 줄바꿈, 가격 잘림이 없도록 Scanner 전용 반응형 CSS를 적용한다.

## 비변경 범위
- KRX 입력 및 B.2.2.2d 무결성 검증
- Scanner 후보 선정 및 Ranking
- 10개 전략 계산
- Risk / Entry / Stop / Target 계산
- Historical Evidence 계산
- Production Exit Policy / Profit Protection
- 전역 Typography / Dark Theme

## 회귀 방지
- 전역 generic `.card`, `.row`, global font selector를 수정하지 않는다.
- B.2.3 CSS는 Scanner 비교/선택 상세 전용 class에만 추가한다.
- 14px 미만 하드코딩 폰트를 추가하지 않는다.
- 기존 날짜 정합성/세션 복원/재분석 흐름을 유지한다.

## 완료 기준
- 후보가 5개여도 전체 상세 카드 5개를 동시에 렌더링하지 않는다.
- 비교 리스트에서 5개 후보 핵심 가격을 즉시 비교할 수 있다.
- 상세 내용은 선택된 후보 1개만 렌더링한다.
- 후보 선택이 세션에 유지된다.
- 새 분석 시 1번 후보가 다시 기본 선택된다.
- 기존 계산 결과 객체를 그대로 표시하며 비즈니스 로직을 변경하지 않는다.
